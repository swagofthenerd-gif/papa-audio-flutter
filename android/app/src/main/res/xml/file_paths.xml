<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- getExternalFilesDir(null)/updates → where we download the update APK -->
    <external-files-path name="updates" path="updates/" />
    <cache-path name="cache" path="." />
</paths>
